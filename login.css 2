           *{
           margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
           }
        
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: aliceblue;
        }
        /* Navbar Styling */
.navbar {
    display: flex;
    align-items: center;
    justify-content: left;
    background: #007bff;
    padding: 10px 20px;
}

/* Logo Styling */
.logo {
    height: 50px; /* Adjust size */
    width: auto;
}

        .login-container {
            background: whitesmoke;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(106, 7, 187, 0.1);
            width: 300px;
            height: 300px;

            text-align: center;
        }
        h2 {
            margin-bottom: 1.5rem;
        }
        .input-group {
            margin-bottom: 1rem;
            text-align: left;
        }
        label {
            display: block;
            font-weight: bold;
        }
        input {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button {
            width: 100%;
            padding: 10px;
            background: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
        }
        button:hover {
            background: #9b0c0c;
        }
        .register-link {
            margin-top: 10px;
            font-size: 0.9rem;
        }
        .register-link a {
            color: #0c74eb;
            font-weight: 100px;
            text-decoration: none;
        }
        .register-link a:hover{
            text-decoration: underline;
        }
